# Roast_My_Resume


The user uploads a PDF in React. Our Node backend extracts the text, chops it into pieces, finds the most relevant parts, and feeds them to an AI model with strict instructions to behave like an angry recruiter.
